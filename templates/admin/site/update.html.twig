{% extends 'base.html.twig' %}

{% block title %}{{ action }} un site{% endblock %}

{% block body %}
    <div class="container container-page admin-page">
        <h1>{{ action }} un site</h1>
        {{  form_start(form) }}

        <div class="form-row">
            {{  form_row(form.nom) }}
        </div>

        <div class="form-row d-flex flex-row gap-3">
            <button class="btn btn-outline-primary"  type="submit">Valider</button>
            <a class="btn btn-outline-danger" href="{{ path('app_admin_site_list') }}">Annuler</a>
        </div>

        {{  form_end(form) }}
    </div>


{% endblock %}